package cse340.menus.enums;

/**
 * This is for the state machine showing the current state of the currently visible menu
 * which could be the START state or SELECTING (the user is currently interacting)
 */
public enum State {
    START,
    SELECTING
}
