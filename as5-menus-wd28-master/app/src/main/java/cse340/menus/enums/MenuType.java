package cse340.menus.enums;

/**
 * There are three types of menus to test in each session:
 * Normal (linear) menus, Pie (round) menus, and a Custom menu that the students make up
 */
public enum MenuType {
    NORMAL,
    PIE,
    CUSTOM
}

